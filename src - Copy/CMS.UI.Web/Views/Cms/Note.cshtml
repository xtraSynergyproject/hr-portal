@using CMS.UI.ViewModel;
@using CMS.Common;
@model NoteTemplateViewModel;
@{
    Layout = ViewBag.Layout;
}
@if (Model.ViewType == NtsViewTypeEnum.Book && Model.DataAction == DataActionEnum.View && Model.ViewMode != NtsViewTypeEnum.Book)
{
    await Html.RenderPartialAsync("NoteBook.cshtml", Model);
}
else
{
    await Html.RenderPartialAsync("NoteDefault.cshtml", Model);
}