@using CMS.Common;
@using Kendo.Mvc.UI;
@model CMS.UI.ViewModel.CandidateEducationalViewModel
@(
                Html.Kendo().DropDownListFor(m=>m)
                    //.Name("")
                    .DataTextField("Name")
                    .DataValueField("Id")
                    .OptionLabel("Select ...")
                    .DataSource(source =>
                    {
                        source.Read(read =>
                        {
                            read.Action("GetEnumIdNameList", "Home", new { Area = "", enumType = "GenderEnum" });
                        });
                    })

)
