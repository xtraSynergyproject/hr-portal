
@using Kendo.Mvc.UI;
@using CMS.UI.ViewModel;

@(
                Html.Kendo().DropDownList()
                    .Name("Pattern")
                    .DataTextField("Name")
                    .DataValueField("Id")
                    .OptionLabel("Select Pattern...")
                    .DataSource(source =>
                    {
                        source.Read(read =>
                        {
                            read.Action("GetCustomIdNameList", "RosterSchedule")/*.Data("getOrgId()")*/;
                        });
                    })

)
