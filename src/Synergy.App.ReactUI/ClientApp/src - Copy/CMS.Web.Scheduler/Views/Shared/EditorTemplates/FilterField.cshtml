
@using Kendo.Mvc.UI;
@using CMS.UI.ViewModel;

@(
                    Html.Kendo().DropDownListFor(m=>m)
                        .DataTextField("title")
                        .DataValueField("name")
                        .BindTo((IEnumerable<DimensionsViewModel>)ViewData["dimensions"]) 

    )
