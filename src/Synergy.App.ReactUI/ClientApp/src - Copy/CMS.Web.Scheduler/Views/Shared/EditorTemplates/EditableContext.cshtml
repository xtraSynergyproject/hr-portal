
@using Kendo.Mvc.UI;
@model CMS.UI.ViewModel.ColumnMetadataViewModel
@*@(
                    Html.Kendo().DropDownListFor(m=>m)
                        //.Name("")
                        .DataTextField("Name")
                        .DataValueField("Id")
                        .OptionLabel("Select ...")
                        .Events(e=>e.Change("onForeignKeyTable"))
                        .DataSource(source =>
                        {
                            source.Read(read =>
                            {
                                read.Action("GetForeignKeyTableList", "TableMetadata", new { Area="Cms"});
                            });
                        })

    )*@
@(Html.Kendo().MultiSelectFor(x => x)
    .AutoClose(false)
    .Placeholder("Select Users...")
   // .OptionLabel("Select ...")
    .DataSource(source =>
    {
        source.Read(read =>
        {
            read.Action("GetUserIdNameList", "User",new { @area="Cms"});
        });

    })
                                                                               //.Events(x=>x.Change("OnChange"))
                                                                               .DataTextField("Name")
                                                                               .DataValueField("Id")
                                                                               .AutoBind(true)
                                                                               .HtmlAttributes(new { @class = "hr-xx-large" })
                                                                                .ValuePrimitive(true)
                                                                               .Height(300)
                                )
