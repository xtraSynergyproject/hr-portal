
@*@using Kendo.Mvc.UI;*@
@model Synergy.App.ViewModel.ColumnMetadataViewModel
@*@(
                Html.Kendo().DropDownListFor(m=>m)
                    //.Name("")
                    .DataTextField("Name")
                    .DataValueField("Id")
                    .OptionLabel("Select ...")
                    //.CascadeFrom("ForeignKeyTableId")
                    .DataSource(source =>
                    {
                        source.Read(read =>
                        {
                            read.Action("GetForeignKeyColumnList", "TableMetadata", new { Area="Cms"}).Data("getTableId()");
                        });
                    })

)*@
