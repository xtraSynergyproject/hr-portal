
@using Kendo.Mvc.UI;
@model Synergy.App.ViewModel.ColumnMetadataViewModel
@(
                Html.Kendo().DropDownListFor(m=>m)
                    //.Name("")
                    .DataTextField("Name")
                    .DataValueField("Id")
                    .OptionLabel("Select ...")
                    .Events(e=>e.Change("onForeignKeyTable"))
                    .DataSource(source =>
                    {
                        source.Read(read =>
                        {
                            read.Action("GetForeignKeyTableList", "TableMetadata", new { Area="Cms"});
                        });
                    })

)
